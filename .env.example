# Supabase Edge Function secrets (set these with `supabase secrets set`; do not add values here)
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
META_APP_SECRET=

# Optional: required only when send-whatsapp-message is called from a browser.
ALLOWED_ORIGIN=

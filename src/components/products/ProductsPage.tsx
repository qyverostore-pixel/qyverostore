import { MessageCircle, Search, SlidersHorizontal, Star } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "@tanstack/react-router";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { cn } from "@/lib/utils";
import { EmptyState, EmptyStateAction } from "@/components/ui/empty-state";
import { ProductsSkeleton } from "@/components/ui/loading-skeletons";
import { useCategories, useProducts } from "@/hooks/use-products";
import type { StoreProduct } from "@/services/products";

type SortOption = "newest" | "price-asc" | "price-desc";

function ProductMedia({ product }: { product: StoreProduct }) {
  const image = product.images[0];
  return <div className="relative aspect-[4/3] overflow-hidden bg-gradient-to-br from-neutral-700 to-neutral-950">{image ? <img src={image.image_url} alt={image.alt_text ?? product.name} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110" /> : <span className="absolute inset-0 grid place-items-center text-display text-4xl font-light tracking-[0.24em] text-white/40">QY</span>}<span className="absolute left-4 top-4 rounded-full border border-white/15 bg-black/25 px-3 py-1 text-[10px] font-medium uppercase tracking-[0.22em] text-foreground/90 backdrop-blur">{product.category?.name ?? "Collection"}</span></div>;
}

function ProductCard({ product }: { product: StoreProduct }) {
  const message = encodeURIComponent(`Hi QYVERO, I'm interested in the ${product.name}.`);
  return <article className="group flex flex-col overflow-hidden rounded-3xl border border-white/10 bg-white/[0.02] shadow-[0_20px_50px_-30px_rgba(0,0,0,.9)] transition-all duration-500 hover:-translate-y-1 hover:border-white/25 hover:bg-white/[0.04] hover:shadow-[0_30px_70px_-30px_rgba(0,0,0,1)]"><ProductMedia product={product} /><div className="flex flex-1 flex-col p-5"><div className="flex items-center gap-1 text-xs text-amber-300"><Star className="h-3.5 w-3.5 fill-current" /><span className="font-semibold">{Number(product.rating).toFixed(1)}</span><span className="text-muted-foreground">({product.reviews_count})</span></div><h2 className="text-display mt-3 text-lg font-medium text-foreground">{product.name}</h2><p className="mt-1 text-base font-semibold text-teal">${Number(product.price)}</p><div className="mt-5 grid gap-2 sm:grid-cols-2"><Link to="/products/$productId" params={{ productId: product.slug }} className="rounded-full bg-foreground px-3 py-2.5 text-xs font-semibold uppercase tracking-[0.16em] text-background transition hover:bg-foreground/90">View Details</Link><a href={`https://wa.me/201505967144?text=${message}`} target="_blank" rel="noreferrer" className="inline-flex items-center justify-center gap-1.5 rounded-full border border-white/15 px-3 py-2.5 text-center text-xs font-medium text-foreground transition hover:border-teal hover:text-teal"><MessageCircle className="h-3.5 w-3.5" />WhatsApp</a></div></div></article>;
}

export function ProductsPage() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All Products");
  const [sort, setSort] = useState<SortOption>("newest");
  const { data: products = [], isLoading } = useProducts();
  const { data: categories = [] } = useCategories();
  const visibleProducts = useMemo(() => products.filter((product) => category === "All Products" || product.category?.name === category).filter((product) => !query.trim() || `${product.name} ${product.category?.name ?? ""}`.toLowerCase().includes(query.trim().toLowerCase())).sort((a, b) => sort === "price-asc" ? Number(a.price) - Number(b.price) : sort === "price-desc" ? Number(b.price) - Number(a.price) : b.created_at.localeCompare(a.created_at)), [category, products, query, sort]);
  const categoryNames = ["All Products", ...categories.map((item) => item.name)];
  return <main className="min-h-screen bg-noise pb-24 pt-12 sm:pb-32 sm:pt-20"><div className="mx-auto w-full max-w-7xl px-6"><div className="mx-auto max-w-2xl text-center"><p className="text-[11px] font-semibold uppercase tracking-[0.4em] text-teal">The Collection</p><h1 className="text-display mt-4 text-4xl font-light leading-[1.05] text-foreground sm:text-6xl">Products made for <span className="italic text-teal">everyday distinction.</span></h1><p className="mt-5 text-base leading-relaxed text-muted-foreground">Explore carefully selected accessories, tech, and essentials for modern everyday life.</p></div><div className="relative mx-auto mt-10 max-w-2xl"><Search className="pointer-events-none absolute left-5 top-1/2 h-5 w-5 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} type="search" placeholder="Search the collection" className="h-14 w-full rounded-full border border-white/15 bg-white/[0.03] pl-13 pr-5 text-sm text-foreground outline-none transition placeholder:text-muted-foreground focus:border-teal focus:ring-2 focus:ring-teal/25" /></div><div className="mt-12 border-y border-white/10 py-5 sm:mt-14"><div className="flex flex-col gap-5 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-center gap-3 overflow-x-auto pb-1 lg:pb-0"><SlidersHorizontal className="h-4 w-4 shrink-0 text-teal" />{categoryNames.map((name) => <button key={name} type="button" onClick={() => setCategory(name)} className={cn("shrink-0 rounded-full border px-4 py-2 text-xs font-medium transition", category === name ? "border-teal bg-teal text-teal-foreground" : "border-white/10 bg-white/[0.02] text-foreground/75 hover:border-white/25 hover:text-foreground")}>{name}</button>)}</div><Select value={sort} onValueChange={(value) => setSort(value as SortOption)}><SelectTrigger aria-label="Sort products" className="h-10 w-full rounded-full border-white/15 bg-white/[0.02] px-4 text-foreground sm:w-52"><SelectValue placeholder="Sort products" /></SelectTrigger><SelectContent className="border-white/10 bg-popover text-foreground"><SelectItem value="newest">Newest</SelectItem><SelectItem value="price-asc">Price: Low to High</SelectItem><SelectItem value="price-desc">Price: High to Low</SelectItem></SelectContent></Select></div></div><div className="mt-7 flex items-center justify-between gap-4"><p className="text-sm text-muted-foreground"><span className="font-semibold text-foreground">{visibleProducts.length}</span> {visibleProducts.length === 1 ? "product" : "products"} found</p>{(query || category !== "All Products") && <button type="button" onClick={() => { setQuery(""); setCategory("All Products"); }} className="text-xs font-medium text-teal transition hover:text-foreground">Clear filters</button>}</div>{isLoading ? <div className="mt-7"><ProductsSkeleton /></div> : visibleProducts.length ? <div className="mt-7 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{visibleProducts.map((product) => <ProductCard key={product.id} product={product} />)}</div> : <div className="mt-7"><EmptyState title="No products found" description="Try a different search or clear your filters to explore the complete QYVERO collection." action={<EmptyStateAction onClick={() => { setQuery(""); setCategory("All Products"); }}>Reset filters</EmptyStateAction>} /></div>}</div></main>;
}

export default ProductsPage;

import { Star } from "lucide-react";
import { useState, type FormEvent } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { cn } from "@/lib/utils";
import { useHasPurchasedProduct, useProductReviews, useReviewMutations } from "@/hooks/use-reviews";
import { useAuth } from "@/providers/AuthProvider";
import type { ProductReview } from "@/services/reviews";
import { useLocale } from "@/providers/LocaleProvider";

function Stars({ rating, interactive = false, onChange }: { rating: number; interactive?: boolean; onChange?: (rating: number) => void }) { const { t } = useLocale(); return <div className="flex gap-1 text-amber-300" aria-label={`${rating} ${t("reviews.ratingOutOfFive")}`}>{Array.from({ length: 5 }, (_, index) => interactive ? <button key={index} type="button" aria-label={`${t("reviews.rate")} ${index + 1} ${t("reviews.stars")}`} onClick={() => onChange?.(index + 1)} className="transition hover:scale-110"><Star className={cn("h-5 w-5", index < rating && "fill-current")} /></button> : <Star key={index} className={cn("h-4 w-4", index < rating && "fill-current")} />)}</div>; }
function ReviewForm({ existing, customerName, onCancel, onSubmit, pending }: { existing?: ProductReview; customerName: string; onCancel?: () => void; onSubmit: (rating: number, review: string) => void; pending: boolean }) { const [rating, setRating] = useState(existing?.rating ?? 5); const [review, setReview] = useState(existing?.review ?? ""); const { t } = useLocale(); const submit = (event: FormEvent) => { event.preventDefault(); onSubmit(rating, review); }; return <form onSubmit={submit} className="rounded-2xl border border-white/10 bg-white/[0.025] p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm font-medium text-foreground">{existing ? t("reviews.edit") : t("reviews.write")}</p><p className="mt-1 text-xs text-muted-foreground">{t("reviews.postingAs")} {customerName}</p></div><Stars rating={rating} interactive onChange={setRating} /></div><Textarea value={review} onChange={(event) => setReview(event.target.value)} maxLength={2000} required placeholder={t("reviews.placeholder")} className="mt-4 min-h-28 border-white/15 bg-black/15" /><div className="mt-4 flex gap-2"><Button type="submit" disabled={pending}>{pending ? t("common.loading") : t("reviews.save")}</Button>{onCancel && <Button type="button" variant="ghost" onClick={onCancel}>{t("common.cancel")}</Button>}</div></form>; }

export function ReviewsSection({ productId }: { productId: string }) { const { user, profile } = useAuth(); const { t } = useLocale(); const { data: reviews = [], isLoading } = useProductReviews(productId); const { data: purchased = false } = useHasPurchasedProduct(productId, user?.id); const { create, update, remove } = useReviewMutations(productId); const [writing, setWriting] = useState(false); const [editingId, setEditingId] = useState<string | null>(null); const customerName = profile?.full_name?.trim() || user?.email?.split("@")[0] || t("reviews.customerFallback"); const ownReview = reviews.find((review) => review.customer_id === user?.id); const save = (rating: number, review: string) => { if (ownReview) update.mutate({ id: ownReview.id, input: { rating, review } }, { onSuccess: () => { setEditingId(null); toast.success(t("reviews.updated")); }, onError: (error) => toast.error(t("reviews.unableUpdate"), { description: error.message }) }); else create.mutate({ productId, rating, review, customerName }, { onSuccess: () => { setWriting(false); toast.success(t("reviews.published")); }, onError: (error) => toast.error(t("reviews.unablePublish"), { description: error.message }) }); }; const deleteOwn = (id: string) => remove.mutate(id, { onSuccess: () => toast.success(t("reviews.deleted")), onError: (error) => toast.error(t("reviews.unableDelete"), { description: error.message }) }); return <section className="py-7"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-display text-2xl font-medium text-foreground">{t("reviews.title")}</h2><p className="mt-1 text-sm text-muted-foreground">{t("reviews.feedback")}</p></div>{purchased && !ownReview && !writing && <Button type="button" variant="outline" onClick={() => setWriting(true)}>{t("reviews.write")}</Button>}</div>{writing && <div className="mt-6"><ReviewForm customerName={customerName} onCancel={() => setWriting(false)} onSubmit={save} pending={create.isPending} /></div>}<div className="mt-6 space-y-4">{isLoading ? <p className="text-sm text-muted-foreground">{t("reviews.loading")}</p> : reviews.length === 0 ? <p className="text-sm text-muted-foreground">{t("reviews.none")}</p> : reviews.map((item) => <article key={item.id} className="rounded-2xl border border-white/10 bg-white/[0.025] p-5">{editingId === item.id ? <ReviewForm existing={item} customerName={customerName} onCancel={() => setEditingId(null)} onSubmit={save} pending={update.isPending} /> : <><div className="flex flex-wrap items-start justify-between gap-3"><div><Stars rating={item.rating} /><p className="mt-3 text-sm font-medium text-foreground">{item.customer_name}</p><p className="mt-1 text-xs text-muted-foreground">{new Date(item.created_at).toLocaleDateString(undefined, { year: "numeric", month: "long", day: "numeric" })}</p></div>{item.customer_id === user?.id && <div className="flex gap-2"><Button type="button" variant="ghost" size="sm" onClick={() => setEditingId(item.id)}>{t("common.edit")}</Button><Button type="button" variant="ghost" size="sm" disabled={remove.isPending} onClick={() => deleteOwn(item.id)}>{t("common.delete")}</Button></div>}</div><p className="mt-4 whitespace-pre-wrap text-sm leading-7 text-foreground/80">{item.review}</p></>}</article>)}</div></section>; }
